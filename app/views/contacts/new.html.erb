<div class="section">
  <div class="container-medium">
  
    <div class="row">
        <div class="d-none d-lg-block col-12 col-lg-6">
            <%= image_tag "contact", class:"img-fluid max-vh-75" %>
        </div>
        <div class="col-12 col-lg-6">
            <h1 class="text-center mb-3">Nous contacter</h1>
            <%= simple_form_for(@contact, url: create_contact_path) do |f| %>
                <div class="mb-4"><%= f.input :first_name, label: "Prénom" %></div>
                <div class="mb-4"><%= f.input :last_name, label: "Nom" %></div>
                <div class="mb-4"><%= f.input :email, label: "Email" %></div>
                <div class="mb-4"><%= f.input :content, label: "Votre message" %></div>
                
                <div class="d-flex justify-content-center">
                    <%= f.submit "Envoyer", class:"btn-egg my-4"%>
                </div>
            <% end %>
        </div>
    </div>
</div>